const chars = '!"#%\'()*+,-./0123456789:;<=>?abcdefghijklmnopqrstuvwxyz[]^_{~}'

const charset = {}
for (const ch of chars) {
  console.log(ch)
}

for (let i = 1; i <= chars.length; i++) {
}

const write_char = (ch: string) => {
}

const write_bool = (b: boolean) => {
}

const write_num = (n: number) => {
}

const flush = () => {
}

function track(event: string, properties?: Object) {
}
